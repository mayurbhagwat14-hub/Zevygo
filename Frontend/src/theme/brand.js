/**
 * Single source of truth for product identity.
 * Prefer importing APP_NAME from here — do not hardcode "ZEVYGO" in UI files.
 */
export const APP_NAME = 'ZEVYGO';

export const APP_TAGLINE = 'Your local services, simplified';

export const APP_DESCRIPTION =
  'ZEVYGO is a hyperlocal multi-service booking platform — find and book trusted local providers in minutes.';

export default APP_NAME;
