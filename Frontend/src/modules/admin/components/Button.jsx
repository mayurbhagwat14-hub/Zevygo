/**
 * Admin Button — thin re-export of the shared ZEVYGO Button.
 * Existing admin imports (`../Button`) keep working without path churn.
 */
export { default } from '../../../components/ui/Button';
